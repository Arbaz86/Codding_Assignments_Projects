import { Heading } from "@chakra-ui/react";
import React from "react";
import { useSelector } from "react-redux";

export const Orders = () => {
  return <Heading>order Placed Successfully 0</Heading>;
};
